ENTRY(start)

virt = 0xC0000000;
phys = 0x100000;

SECTIONS {
    . = virt;
    .text : {
        *(.entry)
        *(.text)
    }
    .data : {
        *(.data)
    }
    .bss : {
        __bss_start = .;
        *(.bss)
        __bss_finish = .;
        __kernel_finish = . + phys - virt;
    }
}
